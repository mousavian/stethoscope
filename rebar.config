{erl_opts, [debug_info]}.
{deps, [
    {ekaf, ".*", {git, "git://github.com/helpshift/ekaf.git"}}
]}.

{relx, [
        {release, { ngnudpserver, "0.1.0" }, [ngnudpserver]},

        {dev_mode, false},
        {include_erts, false}
    ]
}.
